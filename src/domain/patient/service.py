from typing import Optional
from src.domain.patient.schemas.identifier.identifier import Identifier

class PatientRouterService:


	async def add_patient(self, identifier: list[Optional[Identifier]]) -> None:
		pass
